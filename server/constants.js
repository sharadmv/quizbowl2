var init = function(qb) {
  var interface = {
    tier : {
      TEST : "test",
      BETA : "beta",
      RELEASE : "release"
    },
    tag : {
      SERVER : "SERVER"
    }
  }
  return interface;
}
module.exports = init;
